---
import BaseLayout from './BaseLayout.astro';

type Props = {
  title?: string;
};

const { title = 'Dashboard' } = Astro.props as Props;
---

<BaseLayout title={`${title} · CMS Platform`}>
  <section class="space-y-6">
    <header>
      <h1 class="text-3xl font-semibold text-neutral-50">{title}</h1>
      <p class="mt-2 max-w-2xl text-sm text-neutral-400">
        Monitor content performance, collaborate with your team, and keep an eye
        on real-time activity across the platform.
      </p>
    </header>
    <slot />
  </section>
</BaseLayout>
